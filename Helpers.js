
module.exports = Helpers = (() => {
  'use strict';
  const Helpers = {};

  /* Here you can add all the helpers you want or need, and then require them
   * with require('path/to/helpers');
   *
   * Helper Example
   *
   * params:
   *  g -> Gengar instance
   *
   * Helpers.login = function(g) {
   *   g.goTo(PATH);
   *   
   *   // ADD CODE HERE
   * }
   * */

  return Helpers;
})();

